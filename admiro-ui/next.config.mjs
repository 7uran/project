/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    images: {
      domains: ['admin.pixelstrap.net'], 
    },
  };
  
  export default nextConfig;
  